export interface ContentItem {
    id: number,
    video_src?: string;
    source_url?: string;
    alt_text?: string;
    link?: string;
}

export interface Teacher {
    image?: string;
    text?: string;
}